# agentguard.example.conf — Example user config
# Copy this file and pass it with: ./setup.sh user@host --config ./myconfig.conf
# Only include values you want to override from defaults.

# Disable modules you don't need
# MODULE_TSC_GUARD=false

# Swap: use 4GB instead of 8GB
# SWAP_SIZE_GB=4

# tsc-guard: allow 2 concurrent instead of 3
# TSC_GUARD_MAX_SLOTS=2

# tsc-guard: auto-install into these project dirs
# TSC_GUARD_PROJECT_DIRS="/home/ubuntu/myproject /home/ubuntu/other-project"
